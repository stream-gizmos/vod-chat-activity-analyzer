services:
  app:
    build: .
    environment:
      FLASK_ENV:
      FLASK_DEBUG:
    restart: always
  bundler:
    profiles: [ tools ]
    build:
      dockerfile: bundler/Dockerfile
      context: .
    volumes:
      - .:/src/:rw
